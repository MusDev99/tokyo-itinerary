---
import Layout from '../../layouts/Layout.astro';
import PrayerDashboard from '../../components/PrayerDashboard.tsx';
import prayerData from '../../data/japan/prayer.json';
import Header from '../../components/Header.astro';
---

<Layout title="Prayer Times in Japan">
  <Header />
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold text-center mb-8">Prayer Times in Japan</h1>
    <PrayerDashboard prayerData={prayerData} client:load />
  </div>
</Layout>
